# SESH assets

`brand/` contains identity SVGs, `placeholders/` contains local fallbacks, `historical/` contains period treatments, `map/` contains map artwork, and `icons/` contains future icon exports. Remote editorial images are never committed.
